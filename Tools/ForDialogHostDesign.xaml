<Window x:Class="FishRandomSelector.ForDialogHostDesign"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FishRandomSelector"
        mc:Ignorable="d"
        Title="ForDialogHostDesign" Height="450" Width="800"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="导入名单" Grid.Row="0" FontSize="72" HorizontalAlignment="Center" VerticalAlignment="Center" ></TextBlock>
        <TextBlock Text="你可以使用本地文件，将名字用空格分开；或使用.fnamelist文件导入" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24"></TextBlock>
        <Button Content="点击选择文件..." Grid.Row="2" Width="160" HorizontalAlignment="Left"></Button>
        <TextBox Text="..." Grid.Row="1" HorizontalAlignment="Left" Margin="165,29,0,0" Grid.RowSpan="2" Width="635" FontSize="24"></TextBox>
        <TextBox Grid.Row="3" x:Name="textBox" Text="" TextWrapping="Wrap" Width="320" Height="262" HorizontalAlignment="Left" Cursor="IBeam" BorderBrush="{DynamicResource {x:Static SystemColors.ActiveCaptionBrushKey}}" BorderThickness="10,10,10,10"/>
        <Button
            Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
            Command="{x:Static materialDesign:Transitioner.MoveNextCommand}" Margin="0,20,0,0"
            Grid.Row="3">
            <materialDesign:PackIcon Kind="ChevronRight" />
        </Button>
    </Grid>
</Window>
